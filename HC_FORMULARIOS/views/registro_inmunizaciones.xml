<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <record id="view_registro_inmunizacion_form" model="ir.ui.view">
    <field name="name">registro.inmunizacion.form</field>
    <field name="model">registro.inmunizacion</field>
    <field name="arch" type="xml">
      <form string="Registro de Inmunización">
        <header>
           <button
           name="%(HC_FORMULARIOS.action_report_inmunizaciones)d"
           type="action"
           string="Imprimir Evaluación"
           class="btn-primary"
           icon="fa-print"
           />
        </header>
        <sheet>
          <div class="oe_title">
          <h1>
            <field name="name" readonly="1" class="text-primary"/>
          </h1>
        </div>
          <group>
            <field name="retiro_2_id"/>
          </group>

          <notebook>

            <!-- Institución -->
            <page string="Institución">
              <group>
                <field name="institucion_name"/>
                <field name="ruc"/>
                <field name="ciiu"/>
                <field name="establecimiento_salud"/>
              </group>
              <group>
                <field name="historia_clinica"/>
                <field name="numero_archivo"/>
              </group>
            </page>

            <!-- Datos del Paciente -->
            <page string="Paciente">
              <group>
                <field name="primer_apellido"/>
                <field name="segundo_apellido"/>
                <field name="primer_nombre"/>
                <field name="segundo_nombre"/>
              </group>
              <group>
                <field name="sexo"/>
                <field name="fecha_inicio"/>
                <field name="fecha_salida"/>
                <field name="tiempo"/>
                <field name="puesto_trabajo_id"/>
              </group>
            </page>

            <!-- Inmunizaciones -->
            <page string="Inmunizaciones Aplicadas">
              <field name="inmunizaciones_ids">
                <list editable="bottom">
                  <field name="vacuna_id"/>
                  <field name="dosis"/>
                  <field name="fecha"/>
                  <field name="lote"/>
                  <field name="esquema_full"/>
                  <field name="responsable_id"/>
                  <field name="establecimiento_salud"/>
                  <field name="observaciones"/>
                </list>
              </field>
            </page>

          </notebook>
        </sheet>
      </form>
    </field>
  </record>
</odoo>
